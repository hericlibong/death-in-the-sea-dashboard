import * as d3 from 'd3';
import mapboxgl from 'mapbox-gl';
import 'mapbox-gl/dist/mapbox-gl.css';

// Mapbox token will be loaded from an env var (.env) once we wire it.
// Placeholder for Phase 4.
console.log('Death in the Sea — dashboard bootstrapping', { d3Version: d3.version });
